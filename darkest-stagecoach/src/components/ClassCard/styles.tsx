import { Paper } from '@material-ui/core'
import styled from 'styled-components'

export const MainModal = styled(Paper)`
    padding: 2rem;
    background-color: var(--bg-primary);
`

export const ClassCardBanner = styled.div`
    display: flex;
    align-items: center;
    flex-wrap: wrap;
`
